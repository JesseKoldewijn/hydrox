/**
 * Placeholder AppRouter export until nest-native autoSchemaFile regenerates.
 * Web client currently uses an untyped proxy; replace with generated types in CI after API boot.
 */
export type AppRouter = Record<string, unknown>;
